services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: fiber-starter:local
    container_name: fiber-starter
    restart: unless-stopped
    ports:
      - "${APP_HTTP_PORT:-3600}:8080"
    volumes:
      - ./.env.production:/app/.env:ro
    networks:
      - backend

networks:
  backend:
    external: true
