websocket:
  port: ${WEBSOCKET_PORT:6001}
  path: ${WEBSOCKET_PATH:/ws}
  auth_path: /broadcasting/auth
  bus_mode: ${BROADCAST_CONNECTION:redis}
  heartbeat_interval: 30
  write_queue_size: 128
  max_message_size: 65536
  presence_ttl: 120
